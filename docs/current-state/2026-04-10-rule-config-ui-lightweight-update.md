# 2026-04-10 规则配置 UI 轻量化更新

## 本轮目标

把代码走查非法记录页里的 Demo 规则配置区，从“重卡片配置器”收口为更接近轻阿里系的轻量筛选流程。

## 已完成调整

- 保留“先筛选、再预览、最后贴标签”的交互顺序
- 将路径编辑器改成浅灰竖线串联的时间线式结构
- 将过滤步改成自然语言行，输入控件弱化为内联下划线样式
- 弱化大背景块和厚边框，减少框中框
- 保留命中数量可点击的采样预览能力，方便 QA 核对样本

## 当前状态

这轮只调整前端表现层，没有改动规则树内核，也没有改动后端正式判定逻辑。

## 验证

- `npm test` 通过
- `npm run build` 通过

## 备注

- Vite 仍有历史遗留的 chunk 体积 warning
- 该 warning 不是本轮 UI 轻量化调整引入的问题
