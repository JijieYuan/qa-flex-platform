server:
  port: 0

spring:
  datasource:
    url: ${TEST_DATASOURCE_URL:jdbc:postgresql://localhost:15432/qaflex?currentSchema=qaflex_test}
    username: ${TEST_DATASOURCE_USERNAME:qaflex}
    password: ${TEST_DATASOURCE_PASSWORD:qaflex_dev_2026}
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:test-schema-init.sql,classpath:schema.sql
  flyway:
    enabled: false

platform:
  gitlab-mirror:
    scheduler-enabled: false
